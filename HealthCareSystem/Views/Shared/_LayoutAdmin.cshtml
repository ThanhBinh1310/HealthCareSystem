<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - HealthCare Plus</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/styles.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <div class="sidebar-logo">
                <i class="fas fa-shield-alt"></i>
            </div>
            <div class="sidebar-title">Admin Panel</div>
        </div>
        <nav class="sidebar-nav">
            <a asp-controller="Admin" asp-action="Index" class="sidebar-item @(ViewData["ActiveMenu"] == "Dashboard" ? "active" : "")">
                <div class="sidebar-icon">
                    <i class="fas fa-tachometer-alt"></i>
                </div>
                <span class="sidebar-text">Dashboard</span>
            </a>
            <a asp-controller="Admin" asp-action="Users" class="sidebar-item @(ViewData["ActiveMenu"] == "UserManagement" ? "active" : "")">
                <div class="sidebar-icon">
                    <i class="fas fa-users"></i>
                </div>
                <span class="sidebar-text">User Management</span>
            </a>
            @* <a asp-controller="Admin" asp-action="Contents" class="sidebar-item @(ViewData["ActiveMenu"] == "ContentManagement" ? "active" : "")">
                <div class="sidebar-icon">
                    <i class="fas fa-file-alt"></i>
                </div>
                <span class="sidebar-text">Content Management</span>
            </a>
            <a asp-controller="Admin" asp-action="System" class="sidebar-item @(ViewData["ActiveMenu"] == "SystemManagement" ? "active" : "")">
                <div class="sidebar-icon">
                    <i class="fas fa-cogs"></i>
                </div>
                <span class="sidebar-text">System Management</span>
            </a>
            <a asp-controller="Admin" asp-action="Reports" class="sidebar-item @(ViewData["ActiveMenu"] == "Reports" ? "active" : "")">
                <div class="sidebar-icon">
                    <i class="fas fa-chart-bar"></i>
                </div>
                <span class="sidebar-text">Reports</span>
            </a> *@
            <a asp-controller="Admin" asp-action="ManageDoctors" class="sidebar-item @(ViewData["ActiveMenu"] == "Reports" ? "active" : "")">
                <div class="sidebar-icon">
                    <i class="fas fa-users"></i>
                </div>
                <span class="sidebar-text">Doctor Management</span>
            </a>
            <a asp-controller="Admin" asp-action="ManagePatients" class="sidebar-item @(ViewData["ActiveMenu"] == "Reports" ? "active" : "")">
                <div class="sidebar-icon">
                    <i class="fas fa-users"></i>
                </div>
                <span class="sidebar-text">Patient Management</span>
            </a>
            <a asp-controller="Logout" asp-action="Index" class="sidebar-item">
                <div class="sidebar-icon">
                    <i class="fas fa-sign-out-alt"></i>
                </div>
                <span class="sidebar-text">Logout</span>
            </a>
        </nav>
    </div>

    @RenderBody()

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="~/js/site.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
